import { Component } from "@angular/core";

/** Hero section component containing a main headline and call-to-action buttons. */
@Component({
  selector: "app-hero",
  templateUrl: "hero.html",
})
export class Hero {}

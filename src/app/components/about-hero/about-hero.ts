import { Component } from "@angular/core";
import { RouterLink } from "@angular/router";

/** Full-height hero banner for the About / landing page. */
@Component({
  selector: "app-about-hero",
  imports: [RouterLink],
  templateUrl: "about-hero.html",
})
export class AboutHero {}
